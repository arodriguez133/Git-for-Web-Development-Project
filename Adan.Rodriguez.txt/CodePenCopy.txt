https://codepen.io/AdanRod133/pen/ExaVoaZ

 1. What is Semantic HTML? 
 --Semantic HTML adds meaning to a webpage, not just presentation
    2. What is HTML used for? 
 --HTML is used to create electronic documents also known as pages on the World Wide Web
    3. What is an attribute and where do we put it? 
 --Attributes provide additional information about an element 
    4. What is the h1 tag used for? How many times should I use it on a page?
 --The H1 tag is used to identify the most important header on the document 
    5. Name two tags that have required attributes
 --An anchor tag has a required HREF attribute and an img tag as a required SRC attribute
    6. What do we put in the head of our HTML document? 
 --The title and script tags 
    7. What is an id? 
 --It's an attribute that can be applied to any element for styling purposes
    8. What elements can I add an id to? 
 --Any elements
    9. How many times can I use the same id on a page? 
 --It's never ID to have the same Id on multiple elements 
    10. What is a class? 
-- A class is an attribute that specifies one or more class names in an HTML element
    11. What elements can I add a class to? 
-- Any element that makes sense 
    12. How many times can I use the same class on a page? 
-- As many times as makes sense 
    13. How do I get my link to open in a new tab?
-- Using the target = _blank attribute
    14. What is the alt attribute used for? 
-- It gives images more meaning. It may be useful for a screen reader or if the image doesn't run
    15. How do I reference an id?
--an ID can be referenced by the "#" in css
    16. What is the difference between a section and a div
-- A section is an element that makes the markup semantic. A div element is usually used for styling purposes 
    17. What is CSS used for? 
-- To style the page 
    18. How to we select an element? Example - every h2 on the page
-- We select the element by writing it in the css file along with curly brackets
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
-- A class can be used on multiple elements within a section to style a block of html. An ID could be used to select one element to style individually.
    20. How do we select classes in CSS?
-- You select a class using "." followed by the class name and then curly brackets
    21. How do we select a p element with a single class of “human””?
-- "p.human{}"
    22. What is a parent child selector? When would this be useful? 
-- A parent child selector is when you want to style an element within another element. It's useful for styling and specificity
    23. How do you select all links within a div with the class of sidebar?
-- div.sidebar{}
    24. What is a pseudo selector?
-- pseudo selectors are keywords that selects elements in a specific state. It can be used to cut down on the amount of classes you have to use
    25. What do we use the change the spacing between lines?
-- line-height
    26. What do we use to change the spacing between letters?
-- letter-spacing 
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
-- text-transform
    28. How do I add a 1px border around my div that is dotted and black?
--div{border: 1px dotted black;}
    29. How do I select everything on the page? 
-- *{}
    30. How do I write a comment in CSS?
-- /*Like this*/
    31. How do I find out what file I am in, when I am using the command line? 
--In the terminal, the first thing it shows you is what file you are working in 
    32. Using the command line - how do I see a list of files/folders in my current folder?
-- ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
-- rm -- You have to be careful with this because once you enter in the command, the file is gone forever
    34. Why should I use version control? 
-- version control is an efficient was to speed up your workflow. Also, it keeps a record of all changes and help collaboration efforts across your team 
    35. How often should I commit to github?
-- Any time you make a major change. Commit often
    36. What is the command we would use to push our repo up to github? 
-- git push
    37. Walk me through Lambda's git flow. 
    Find lambdas master branch> fork it> clone it> open git bash and git clone [paste link here]> use "git checkout -b [firtname-lastname]" to create a new branch from YOUR master branch> Add, commit, and push changes.> make a pull request> add yout TL/PM as collaborator>TL/PM will review code and accept merge request>
